git init
git add .
git commit -m "inital commit"
git remote add origin https://github.com/SigmaDagger005/fsd_26.git
git branch -M main
git push -u origin main


git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

to set your account's default identity.
Omit --global to set the identity only in this repository.

----git repeats-----
git add .
git commit -m "message"
git push

------GitHub Pages---------
any static website can be deployed by gitHub pages
repository>setting>pages>deploy from branch> select branch and save
wait 2 minutes then refresh